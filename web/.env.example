# API base URL (no trailing slash). Use the Invoke URL from API Gateway Stages.
# HTTP API with $default stage: https://xxxx.execute-api.region.amazonaws.com
# Named stage (e.g. staging):     https://xxxx.execute-api.region.amazonaws.com/staging
VITE_API_BASE_URL=
